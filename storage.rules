rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /suggestions/{imageName} {
      allow read, write: if request.auth != null
        && request.auth.token.email == 'hyde@hvshavn.dk';
    }
  }
}